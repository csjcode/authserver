
export function signinUser({ email, pasword }) {
  // Submit to server
  // If good:
  // Update state to Authenticated
  // Save JWT token
  // redirect to the route /feature

  // If request is Based
  // Show an error
};
